version: "3.6"

services:
    web:
        build: .
        env_file:
            - .env
        ports:
            - "${NODE_PORT}:${NODE_PORT}"
        volumes:
            - .:/home/app
            - ~/.npm:/.npm
